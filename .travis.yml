language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t kewlpack/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kewlpack/docker-react npm run test
